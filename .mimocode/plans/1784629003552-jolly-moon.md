# Security Fixes + Bug Fix Plan

## Issue 1: Products not showing on /products page
**Root cause:** Local Express server running old code (returns array instead of paginated object). The new `api/products.js` with pagination hasn't been restarted.
**Fix:** Restart the Express server. Also need to update the local server's products route.

## Issue 2: Rate Limiting for Login
**File: `api/_lib/rateLimit.js`** (NEW)
- In-memory rate limiter tracking failed login attempts per IP
- 5 max attempts per 15 minutes
- Returns 429 with remaining lockout time

**File: `api/auth.js`** (MODIFY)
- Add rate limit check before password comparison
- Return 429 with retry-after when blocked

## Issue 3: Security Headers
**File: `vercel.json`** (MODIFY)
- Remove `Access-Control-Allow-Origin: *`
- Add restrictive CORS for frontend domain only
- Add security headers (X-Content-Type-Options, X-Frame-Options, etc.)

## Issue 4: Input Validation
**File: `api/auth.js`** (MODIFY)
- Validate email format
- Validate password length
- Sanitize inputs

## Files to create/modify:
1. `api/_lib/rateLimit.js` - NEW
2. `api/auth.js` - MODIFY (add rate limiting + validation)
3. `vercel.json` - MODIFY (security headers)
4. `server/index.js` - UPDATE products route to match new paginated format

## Verification:
1. Restart Express server → products should show
2. Test login 5 times with wrong password → 6th attempt returns 429
3. Check security headers in browser DevTools
